# 执行顺序大于application.properties
# 通常覆盖application.yml [如果存在]中的内容
# 构建配置中心
spring:
  application:
    name: eureka-web
  cloud:
    config:
      uri: http://localhost:8764/CONFIG-SERVER/
#        searchPaths: spring-cloud-demo
      profile: default
      label: master
server:
  port: 8771
